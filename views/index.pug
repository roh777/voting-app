extends layout

block content
    h1 Welcome to Pollster.
    p Create and share polls to collect insights and opinions.

    h2 Recent Polls
    .recentpolls
        each poll in polls
            .question 
                a(href=`/poll/${poll._id}`) #{poll.question}
    